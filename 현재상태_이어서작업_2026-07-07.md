# 핑(Ping) — 노트북2 이어서작업 스냅샷

> 작성: 2026-07-07 · 노트북1 작업분이 구글드라이브+git으로 동기화된 시점 기준.
> **깊은 내용은 `HANDOFF.md`** 가 정본입니다. 이 파일은 "지금 어디까지 왔고, 노트북2에서 뭘 먼저 하면 되는지"만 빠르게 잡는 용도.

## 한 줄 요약
뚜비네 가족 라이브 PWA **핑**. 현재 라이브 = **v9**(산책·모임 체크 AI 진위 검증까지 배포 완료). 코드는 origin/main과 동기화된 안정 상태이고, **핑1은 유지보수(동결) 모드** — 신규 기능 멈추고 버그만 수정, 리소스는 핑2 4주 스프린트로.

## 동기화 / git 상태 (지금)
- 브랜치 `main`, **origin/main과 일치**(로컬 미푸시 커밋 없음).
- 최신 커밋: **`bd4f089` "v9: 산책·모임 체크 AI 진위 검증"**.
- 워킹트리 깨끗함. **단, 커밋 안 된 파일 1개**: `작업규칙_모델설정.md`(untracked, 2026-07-07 생성). → 내부 문서라 `.gitignore` 대상은 아님. 노트북2에서 커밋할지 결정 필요(내부 메모라 안 올려도 무방).
- 라이브 버전 표식 일치 확인: `sw.js` CACHE_NAME=`ping-v9`, `app.html` `#verStamp`=`v9`.

## 지금 라이브에 올라가 있는 것 (v9)
- 홈 피드 컴팩트 리디자인(v7) + 모바일 카드 잘림 버그 수정 완료.
- 오늘 핑 **산책·모임 체크 수정** 기능(v8).
- **AI 진위 검증**(v9): 산책=강아지 감지 / 모임=사람 3명↑. Cloud Function `verifyPingPhoto`(Gemini 2.5-flash) 배포 완료. 실패·오프라인 시 통과 처리라 핑 흐름은 안 막힘.
- Cloud Functions 5개 배포됨(asia-northeast3, Node22/2nd Gen): `onPing`, `onPoke`, `coachReminder`(18시), `deadlineReminder`(21:30), `verifyPingPhoto`.

## 노트북2에서 먼저 할 일 (순서대로)
1. **git 상태 재확인** — PC 파워셸에서 `git status` / `git log --oneline -3`으로 `bd4f089`까지 동기화됐는지 확인.
2. **문서 읽는 순서**: 이 파일 → `HANDOFF.md`(정본, 전체 맥락) → `작업규칙_모델설정.md`(모델 오케스트레이션) → `핑1_할일_2026-07.md`(핑1 스프린트 할 일).
3. **`작업규칙_모델설정.md` 처리** — 커밋할지 결정. 그리고 문서 지시대로 **앱 설정 → 개인 지침에도 같은 규칙을 넣어야** 모든 세션에 자동 적용(사용자가 직접 저장).
4. 코드 수정 재개 전 **배포 규칙 숙지**: `DEPLOY.md`. 배포/커밋/푸시는 **PC 파워셸에서 사용자가 직접**(샌드박스에서 git commit/push 금지 — 드라이브 `.lock` 잔류로 PC git 막힘).

## 남은 할 일 / 다음 후보 (요약)
핑1은 동결 모드라 아래는 대부분 **관찰·검증 위주**, 신규 개발은 4주 게이트 이후.
- **v9 실기기 테스트**: 뚜비 없는 사진에 산책 체크 → "해제" 토스트 확인 / 뚜비 사진 통과 / 체크수정·사진교체 재검증.
- v7 컴팩트 리디자인 + 모바일 잘림수정 가족 실기기 재확인, UX 피드백 수집.
- **크롬 정착 안내**: 가족이 카톡 인앱 말고 크롬/설치앱으로 쓰게(캐시 고착 방지).
- 푸시 20종 문구·타이밍·우선순위 피드백(오작동만 수정).
- 릴스/쇼츠 생산 루틴(`핑_릴스쇼츠_광고기획안.docx` 22편) — 핑1 = 촬영 스튜디오.
- **게이트 이후로 미룸**: 600 간식 "대장 선택" 부활(`choices`+상품 소싱), 접근성 minor 3건, 검증 기능의 핑2 이식.

## 작업 규칙 리마인더 (모델 오케스트레이션)
- 메인 = **Fable 5**(지휘: 계획·분배·판단·종합).
- 무거운 추론·설계·근본원인, 코드 구현·수정·테스트·리뷰 → **Opus 4.8** 하위 에이전트 위임(추론 예산 높게).
- 단순 실행·조회·검색 잡무 → **Haiku 4.5** 하위 에이전트. 단 1~2개 파일 사소한 수정·md/json·단순 조회는 메인이 직접.
- 핑1(`dev/핑`)·핑2(`dev/핑2`) 공통 적용. (※ 이 세션 마운트에는 **핑 폴더만** 있음 — 핑2는 별도 폴더/세션.)

## 주의 (코드 만질 때)
- `app.html` **CRLF 줄바꿈** + 파일이 큼(~186KB) → bash가 중간 truncate 하는 경우 있음. 검증은 신규 로직만 heredoc으로 `node --check`, 통합 부위는 Read 도구로. Edit/Read는 정상.
- 배포마다 `sw.js` CACHE_NAME 숫자 + `#verStamp` 텍스트 **같이** 올리기.
- 핑2 멀티테넌시 `firestore.rules`를 핑1(ping-family) 프로젝트에 배포 금지. 핑1 실제 가족 데이터 삭제 금지.

## 참고 문서 맵
- `HANDOFF.md` — 전체 정본(기능·데이터·Firebase·이미지·세션 로그).
- `작업규칙_모델설정.md` — 모델 오케스트레이션 규칙.
- `핑1_할일_2026-07.md` — 핑1 스프린트 할 일 / 핑1↔핑2 경계.
- `DEPLOY.md` — 배포 절차·lock 에러 대처. `FCM_SETUP.md` — 푸시 설정.
- `데이터구조_설계.md`·`ping-service/` — 핑2/서비스화(라이브와 분리, .gitignore).
