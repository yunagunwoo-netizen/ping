# 작업 모델 오케스트레이션 규칙 (김아빠)

> 2026-07-07 갱신. 어느 노트북/세션이든 이 규칙을 따른다.
> 전역 적용: 앱 설정 → **개인 지침**에도 같은 문구를 넣어야 모든 코워크 세션에 자동 적용된다(설정 저장은 사용자가 직접).

## 역할 분담 — Fable 지휘 · Opus 실행 · Haiku 잡무
- **메인 세션 모델 = Fable 5 (지휘자).** 계획·작업 분배·중요한 판단·결과 종합만 직접 한다. (앱 상단 모델 선택기를 Fable 5로 둔다.)
- **무거운 추론·설계·아키텍처·근본원인 분석 → Opus 4.8 하위 에이전트**에 위임(추론 예산 높게).
- **코드 구현·수정·테스트·디버깅·리뷰 → Opus 4.8 하위 에이전트**에 위임.
- **단순 명령 실행·빌드·파일/로그 조회·검색 같은 잡무 → Haiku 4.5 하위 에이전트**에 위임.
- 단, **1~2개 파일의 사소한 수정·설정/문서(md·json 등) 변경·단순 조회는 메인이 직접** 처리(왕복 비용 절약).
- 독립 작업은 병렬로 위임하고, 위임 중에도 계속 진행한다.

## 적용 방법
- 코워크에는 Claude Code식 **강제 차단 훅(PreToolUse)이 없다** → 위 규칙은 권고로 운영(물리 차단 아님).
- 세션이 가볍고 단순하면 모델 선택기를 낮춰(예: Opus/Haiku) 써도 된다.
- 두 프로젝트(핑1 `dev/핑` · 핑2 `dev/핑2`) 공통 적용. 드라이브 동기화라 노트북1·2 모두 반영.

## 배경
- 이 방향은 successwiki 'Fable 5 오케스트레이션 가이드'(Claude Code CLI 전용: CLAUDE.md·서브에이전트·env 래퍼·PreToolUse 게이트)의 원리를 코워크에 맞게 옮긴 것.
- Claude Code 터미널 환경이라면 그 가이드대로 훅·게이트까지 설치해 물리 차단을 걸 수 있음(코워크에선 불가).

## 이전 규칙 대체
- 2026-07-06 이전의 "자료조사=Opus / 코딩=Fable" 규칙은 위 오케스트레이션 방식으로 **대체**됨.
